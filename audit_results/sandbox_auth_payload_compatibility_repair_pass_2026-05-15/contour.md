# Contour

SANDBOX_AUTH_PAYLOAD_COMPATIBILITY_REPAIR_PASS

Goal: repair sandbox auth payload compatibility with the sandbox owner onboarding lane so the next onboarding rerun can reach reserve-first evaluation instead of stopping at auth-type mismatch.
