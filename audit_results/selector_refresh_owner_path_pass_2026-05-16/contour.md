# SELECTOR_REFRESH_OWNER_PATH_PASS

## Goal

Use the exact canonical owner refresh surface for selected-backend participation
evidence, then determine whether refreshed selector truth becomes fresh and
usable for the next exact-source contour or instead exposes a new contradiction.

## Scope

- in scope:
  - owner-refresh surface naming
  - `sync --json` as the selected-backend snapshot refresh owner path
  - post-refresh `rollout rotation inspect --json`
  - post-refresh `status --json`
  - exact-source-earning decision
- out of scope:
  - sandbox `auth.json` materialization
  - onboarding rerun
  - exact auth-source admission
  - manual supervisor-state edits

## Result

- canonical owner refresh surface named as `sync --json`
- selector snapshot refresh succeeded and became fresh
- refreshed selector truth became contradicted by policy drift rather than
  narrowing to an exact source
- next contour is a diagnose contour, not exact-source admission
