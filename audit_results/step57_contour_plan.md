CONTOUR:
ID:
ROTATION_EVIDENCE_STALE_RECLEAR_CONTOUR

Goal:
Refresh selected-backend snapshot participation evidence through the sync owner
lane, removing `ROTATION_EVIDENCE_STALE` without expanding this contour into
posture normalization, stage-20, same-day validation, or UI.

Execution summary:
- capture fresh preflight packets
- confirm `sync --json` owner-lane admissibility
- declare one exact live operation
- execute exactly one owner-way write step if canon-backed
- reread and close with factual rotation-evidence verdict
