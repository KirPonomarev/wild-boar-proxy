# DEEPSEEK_DIRECT_PROVIDER_ROUTE_PASS Spec

Add a direct DeepSeek provider lane without browser secret intake. The route uses `https://api.deepseek.com`, `/chat/completions`, `deepseek-chat`, and `DEEPSEEK_API_KEY`.
