CONTOUR:
`OWNER_SURFACE_CONTRADICTION_REPAIR_CONTOUR`

Goal:
- repair direct-active materialization outside reserve-first owner truth

Bounded target:
- repo-side onboarding owner path must not claim reserve-first success if
  post-sync truth moves the selected backend into `active` or selected routing

Primary contradiction chain:
1. external onboarding admits a new backend to `reserve`
2. external sync may auto-promote that backend into `active`
3. repo owner surface must not close as reserve-first success afterward

In scope:
- targeted repo/runtime repair
- targeted test harness support for registry+state sync mutations
- focused regression pack
- independent inspection

Out of scope:
- live mutation
- stage-20
- same-day validation
- UI
